version: "3.9"

services:
  webgpu-dev:
    build: .
    ports:
      - "5173:5173"  # Vite's default dev server port
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    command: vite --host
